@model CinemaAdmin.Administration.Models.OccupationModel

@{
    ViewBag.Title = "OccupationView";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@if (HttpContext.Current.Request.Cookies.AllKeys.Contains("loginCookie"))
{
    if (Request.Cookies["workCookie"].Value == "manager")
    {
        <html>
        <body>
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3>Selecteer datum</h3>
                </div>
                <div class="panel-body">
                    @using (Html.BeginForm("OccupationView", "Occupation"))
                    {
                        <div class="form-group row">
                            <input type="date" name="date1" id="time" value="">
                        </div>
                        <div class="form-group row">
                            <input type="date" name="date2" id="time" value="">
                        </div>
                        <button type="submit" class="btn btn btn-primary" value="" style="width:100%">Weergeef</button>
                    }
                </div>
            </div>

        </body>
    </html>
    }
}